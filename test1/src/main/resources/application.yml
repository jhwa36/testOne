spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jpaProject
    username: postgres
    password: apfhd1
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
